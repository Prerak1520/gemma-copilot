# Local data

This directory is for local runtime data.

`data/wiki-snapshot/` is intentionally ignored because it can contain private career notes, feedback history, Obsidian metadata, and other personal context. Recreate it locally with:

```bash
npm run init-wiki
```
