PORT=3939
API_ALLOWED_ORIGINS=http://localhost:3940
OLLAMA_URL=http://localhost:11434
EDGE_MODEL=gemma4:e2b
DEEP_MODEL=gemma4:26b

# Optional: set to a vision-capable Ollama model to extract text when extension
# screenshot fallback is used and the DOM scrape is too short.
SCREENSHOT_MODEL=

# Optional: override where the local SQLite database is stored.
DB_PATH=./data/copilot.db

# Optional: source wiki copied by npm run init-wiki.
WIKI_SOURCE=/Users/prerak/claude /career-wiki
